# Local Development Configuration
spring.application.name=task-manager

# Local MongoDB (if you have local MongoDB, otherwise use cloud)
spring.data.mongodb.uri=mongodb+srv://nitish_db_user:nitish%401231@task-manager-cluster.ggcoxrj.mongodb.net/taskdb
spring.data.mongodb.database=taskdb

# Local Server Port
server.port=8080

# Local CORS - Allow local frontend
cors.allowed.origins=http://localhost:5173,http://localhost:3000,http://127.0.0.1:5173

# Enable debug logging for development
logging.level.com.nitish.task_manager=DEBUG
logging.level.org.springframework.web=DEBUG
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} - %msg%n
