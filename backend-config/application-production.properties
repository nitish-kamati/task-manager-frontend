# Production Configuration
spring.application.name=task-manager

# Production MongoDB - Use environment variables for security
spring.data.mongodb.uri=${MONGODB_URI:mongodb+srv://nitish_db_user:nitish%401231@task-manager-cluster.ggcoxrj.mongodb.net/taskdb}
spring.data.mongodb.database=${MONGODB_DATABASE:taskdb}

# Production Server Port - Render provides PORT
server.port=${PORT:8080}

# Production CORS - Allow production frontend
cors.allowed.origins=https://task-manager-frontend-drab-one.vercel.app,https://*.vercel.app

# Production Logging
logging.level.com.nitish.task_manager=INFO
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} - %msg%n
